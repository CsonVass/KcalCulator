version: '3.4'

services:
  nutrientservice.api:
    image: ${DOCKER_REGISTRY-}nutrientserviceapi
    build:
      context: .
      dockerfile: Nutrient_info_service\nis_api\Dockerfile


  mongodb_container:
    image: mongo:latest
    container_name: nutrient-db
    ports:
      - 27017:27017

      